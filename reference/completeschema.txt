-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.activity_feed (
  id integer NOT NULL DEFAULT nextval('activity_feed_id_seq'::regclass),
  user_id integer NOT NULL,
  activity_type character varying NOT NULL,
  entity_type character varying,
  entity_id integer,
  activity_data json,
  visibility character varying DEFAULT 'public'::character varying,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT activity_feed_pkey PRIMARY KEY (id),
  CONSTRAINT activity_feed_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.campaign_embeddings (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid,
  embedding USER-DEFINED,
  metadata jsonb,
  version integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT campaign_embeddings_pkey PRIMARY KEY (id),
  CONSTRAINT campaign_embeddings_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id)
);
CREATE TABLE public.campaigns (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  company_id integer,
  cause text NOT NULL,
  region text NOT NULL,
  budget_inr numeric,
  timeline integer,
  title text,
  description text,
  budget_breakdown jsonb,
  schedule_vii text,
  sdg_alignment ARRAY,
  impact_metrics jsonb,
  milestones jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT campaigns_pkey PRIMARY KEY (id)
);
CREATE TABLE public.company_verifications (
  id integer NOT NULL DEFAULT nextval('company_verifications_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  company_name character varying,
  registration_number character varying,
  gst_number character varying,
  verification_status character varying DEFAULT 'unverified'::character varying,
  verification_date timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  sector character varying,
  CONSTRAINT company_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT company_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.csr_audit_log (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  entity_type text NOT NULL,
  entity_id uuid NOT NULL,
  event_type text NOT NULL,
  event_hash text NOT NULL,
  event_payload jsonb DEFAULT '{}'::jsonb,
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_audit_log_pkey PRIMARY KEY (id),
  CONSTRAINT csr_audit_log_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.csr_impact_metrics (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  project_id uuid NOT NULL,
  campaign_id uuid NOT NULL,
  beneficiaries integer DEFAULT 0,
  funds_utilized numeric DEFAULT 0,
  progress_percentage integer DEFAULT 0,
  custom_metrics jsonb DEFAULT '{}'::jsonb,
  last_updated timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_impact_metrics_pkey PRIMARY KEY (id),
  CONSTRAINT csr_impact_metrics_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT csr_impact_metrics_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id)
);
CREATE TABLE public.csr_milestone_evidence (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  milestone_id uuid NOT NULL,
  project_id uuid NOT NULL,
  uploaded_by integer NOT NULL,
  ngo_user_id integer NOT NULL,
  device_id text NOT NULL,
  submission_status text NOT NULL DEFAULT 'submitted'::text,
  description text,
  gps_lat numeric,
  gps_long numeric,
  gps_accuracy_meters numeric,
  captured_at timestamp with time zone NOT NULL,
  uploaded_at timestamp with time zone DEFAULT now(),
  evidence_summary jsonb DEFAULT '{}'::jsonb,
  immutable_hash text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_milestone_evidence_pkey PRIMARY KEY (id),
  CONSTRAINT csr_milestone_evidence_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id),
  CONSTRAINT csr_milestone_evidence_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT csr_milestone_evidence_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES public.users(id),
  CONSTRAINT csr_milestone_evidence_ngo_user_id_fkey FOREIGN KEY (ngo_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.csr_milestone_evidence_documents (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  evidence_id uuid NOT NULL,
  document_url text NOT NULL,
  file_name text NOT NULL,
  mime_type text,
  file_size_bytes bigint,
  uploaded_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_milestone_evidence_documents_pkey PRIMARY KEY (id),
  CONSTRAINT csr_milestone_evidence_documents_evidence_id_fkey FOREIGN KEY (evidence_id) REFERENCES public.csr_milestone_evidence(id)
);
CREATE TABLE public.csr_milestone_evidence_media (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  evidence_id uuid NOT NULL,
  media_type text NOT NULL,
  media_url text NOT NULL,
  mime_type text,
  file_name text,
  file_size_bytes bigint,
  captured_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_milestone_evidence_media_pkey PRIMARY KEY (id),
  CONSTRAINT csr_milestone_evidence_media_evidence_id_fkey FOREIGN KEY (evidence_id) REFERENCES public.csr_milestone_evidence(id)
);
CREATE TABLE public.csr_milestone_reviews (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  milestone_id uuid NOT NULL,
  evidence_id uuid,
  reviewer_id integer NOT NULL,
  decision text NOT NULL,
  comments text,
  reviewed_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_milestone_reviews_pkey PRIMARY KEY (id),
  CONSTRAINT csr_milestone_reviews_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id),
  CONSTRAINT csr_milestone_reviews_evidence_id_fkey FOREIGN KEY (evidence_id) REFERENCES public.csr_milestone_evidence(id),
  CONSTRAINT csr_milestone_reviews_reviewer_id_fkey FOREIGN KEY (reviewer_id) REFERENCES public.users(id)
);
CREATE TABLE public.csr_payment_confirmations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  milestone_id uuid NOT NULL,
  project_id uuid NOT NULL,
  payment_reference text NOT NULL,
  receipt_url text,
  amount numeric NOT NULL,
  payment_status text NOT NULL DEFAULT 'pending'::text,
  confirmed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_payment_confirmations_pkey PRIMARY KEY (id),
  CONSTRAINT csr_payment_confirmations_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id),
  CONSTRAINT csr_payment_confirmations_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id)
);
CREATE TABLE public.csr_project_milestones (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  project_id uuid NOT NULL,
  campaign_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  milestone_order integer NOT NULL,
  amount numeric NOT NULL,
  evidence_requirements jsonb NOT NULL DEFAULT '[]'::jsonb,
  status text NOT NULL DEFAULT 'pending'::text,
  due_date date,
  company_approval_required boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_project_milestones_pkey PRIMARY KEY (id),
  CONSTRAINT csr_project_milestones_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT csr_project_milestones_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id)
);
CREATE TABLE public.csr_projects (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  campaign_id uuid NOT NULL,
  company_user_id integer NOT NULL,
  ngo_user_id integer NOT NULL,
  title text NOT NULL,
  description text,
  region text,
  project_status text NOT NULL DEFAULT 'active'::text,
  acceptance_date timestamp with time zone,
  start_date date,
  end_date date,
  total_budget numeric,
  funds_utilized numeric DEFAULT 0,
  progress_percentage integer DEFAULT 0,
  expected_beneficiaries integer,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT csr_projects_pkey PRIMARY KEY (id),
  CONSTRAINT csr_projects_campaign_id_fkey FOREIGN KEY (campaign_id) REFERENCES public.campaigns(id),
  CONSTRAINT csr_projects_company_user_id_fkey FOREIGN KEY (company_user_id) REFERENCES public.users(id),
  CONSTRAINT csr_projects_ngo_user_id_fkey FOREIGN KEY (ngo_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.email_verifications (
  id bigint NOT NULL DEFAULT nextval('email_verifications_id_seq'::regclass),
  user_id bigint NOT NULL,
  token character varying NOT NULL UNIQUE,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT email_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT email_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.evidence_validation_results (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  evidence_id uuid NOT NULL,
  project_id uuid NOT NULL,
  milestone_id uuid NOT NULL,
  validation_status text NOT NULL DEFAULT 'pending'::text,
  min_evidence_count_ok boolean,
  geo_within_region_ok boolean,
  captured_after_project_start_ok boolean,
  gps_accuracy_ok boolean,
  device_assignment_ok boolean,
  notes text,
  validation_payload jsonb DEFAULT '{}'::jsonb,
  validated_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT evidence_validation_results_pkey PRIMARY KEY (id),
  CONSTRAINT evidence_validation_results_evidence_id_fkey FOREIGN KEY (evidence_id) REFERENCES public.csr_milestone_evidence(id),
  CONSTRAINT evidence_validation_results_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT evidence_validation_results_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id)
);
CREATE TABLE public.field_devices (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  device_id text NOT NULL UNIQUE,
  ngo_user_id integer NOT NULL,
  assigned_user_id integer,
  device_label text,
  platform text,
  app_version text,
  status text NOT NULL DEFAULT 'active'::text,
  registered_at timestamp with time zone DEFAULT now(),
  last_seen_at timestamp with time zone,
  revoked_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT field_devices_pkey PRIMARY KEY (id),
  CONSTRAINT field_devices_ngo_user_id_fkey FOREIGN KEY (ngo_user_id) REFERENCES public.users(id),
  CONSTRAINT field_devices_assigned_user_id_fkey FOREIGN KEY (assigned_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.field_sync_receipts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  client_submission_uuid uuid NOT NULL UNIQUE,
  project_id uuid NOT NULL,
  milestone_id uuid,
  evidence_id uuid,
  ngo_user_id integer NOT NULL,
  device_id text NOT NULL,
  batch_id uuid,
  receipt_hash text NOT NULL,
  sync_status text NOT NULL DEFAULT 'received'::text,
  received_at timestamp with time zone DEFAULT now(),
  processed_at timestamp with time zone,
  server_message text,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT field_sync_receipts_pkey PRIMARY KEY (id),
  CONSTRAINT field_sync_receipts_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT field_sync_receipts_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id),
  CONSTRAINT field_sync_receipts_evidence_id_fkey FOREIGN KEY (evidence_id) REFERENCES public.csr_milestone_evidence(id),
  CONSTRAINT field_sync_receipts_ngo_user_id_fkey FOREIGN KEY (ngo_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.hashtags (
  id integer NOT NULL DEFAULT nextval('hashtags_id_seq'::regclass),
  tag character varying NOT NULL UNIQUE,
  total_mentions integer DEFAULT 0,
  weekly_mentions integer DEFAULT 0,
  daily_mentions integer DEFAULT 0,
  trending_score numeric DEFAULT 0,
  category character varying DEFAULT 'general'::character varying,
  is_trending boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT hashtags_pkey PRIMARY KEY (id)
);
CREATE TABLE public.individual_verifications (
  id integer NOT NULL DEFAULT nextval('individual_verifications_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  aadhaar_number character varying,
  aadhaar_verified boolean DEFAULT false,
  aadhaar_verification_date timestamp without time zone,
  pan_number character varying,
  pan_verified boolean DEFAULT false,
  pan_verification_date timestamp without time zone,
  verification_status character varying DEFAULT 'unverified'::character varying,
  verification_date timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT individual_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT individual_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.ngo_verifications (
  id integer NOT NULL DEFAULT nextval('ngo_verifications_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  ngo_name character varying,
  registration_number character varying,
  registration_type character varying,
  fcra_number character varying,
  verification_status character varying DEFAULT 'unverified'::character varying,
  verification_date timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  sector character varying,
  CONSTRAINT ngo_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT ngo_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.phone_verifications (
  id bigint NOT NULL DEFAULT nextval('phone_verifications_id_seq'::regclass),
  user_id bigint NOT NULL,
  phone character varying NOT NULL,
  otp character varying NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT phone_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT phone_verifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.platform_announcements (
  id text NOT NULL,
  type text NOT NULL CHECK (type = ANY (ARRAY['announcement'::text, 'changelog'::text])),
  title text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT platform_announcements_pkey PRIMARY KEY (id)
);
CREATE TABLE public.post_comments (
  id bigint NOT NULL DEFAULT nextval('post_comments_id_seq'::regclass),
  post_id bigint NOT NULL,
  author_id integer NOT NULL,
  parent_comment_id bigint,
  content text NOT NULL,
  media_urls jsonb,
  mentioned_users jsonb,
  reaction_count integer DEFAULT 0,
  reply_count integer DEFAULT 0,
  status USER-DEFINED DEFAULT 'active'::comment_status_enum,
  is_flagged boolean DEFAULT false,
  flagged_reason text,
  moderated_at timestamp with time zone,
  moderated_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT post_comments_pkey PRIMARY KEY (id),
  CONSTRAINT post_comments_post_id_fkey FOREIGN KEY (post_id) REFERENCES public.posts(id),
  CONSTRAINT post_comments_author_id_fkey FOREIGN KEY (author_id) REFERENCES public.users(id),
  CONSTRAINT post_comments_parent_comment_id_fkey FOREIGN KEY (parent_comment_id) REFERENCES public.post_comments(id),
  CONSTRAINT post_comments_moderated_by_fkey FOREIGN KEY (moderated_by) REFERENCES public.users(id)
);
CREATE TABLE public.post_interactions (
  id integer NOT NULL DEFAULT nextval('post_interactions_id_seq'::regclass),
  post_id integer NOT NULL,
  user_id integer NOT NULL,
  interaction_type character varying NOT NULL,
  interaction_value integer DEFAULT 1,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT post_interactions_pkey PRIMARY KEY (id),
  CONSTRAINT post_interactions_post_id_fkey FOREIGN KEY (post_id) REFERENCES public.posts(id)
);
CREATE TABLE public.post_reactions (
  id bigint NOT NULL DEFAULT nextval('post_reactions_id_seq'::regclass),
  post_id bigint NOT NULL,
  user_id integer NOT NULL,
  reaction_type USER-DEFINED DEFAULT 'like'::reaction_type_enum,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT post_reactions_pkey PRIMARY KEY (id),
  CONSTRAINT post_reactions_post_id_fkey FOREIGN KEY (post_id) REFERENCES public.posts(id),
  CONSTRAINT post_reactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.posts (
  id bigint NOT NULL DEFAULT nextval('posts_id_seq'::regclass),
  author_id integer NOT NULL,
  content text NOT NULL,
  post_type USER-DEFINED DEFAULT 'text'::post_type_enum,
  media_urls jsonb,
  media_metadata jsonb,
  visibility USER-DEFINED DEFAULT 'public'::content_visibility_enum,
  allow_comments boolean DEFAULT true,
  allow_reactions boolean DEFAULT true,
  is_pinned boolean DEFAULT false,
  is_featured boolean DEFAULT false,
  reaction_count integer DEFAULT 0,
  comment_count integer DEFAULT 0,
  share_count integer DEFAULT 0,
  view_count integer DEFAULT 0,
  tags jsonb,
  mentioned_users jsonb,
  category character varying,
  location character varying,
  organization_context jsonb,
  is_flagged boolean DEFAULT false,
  is_approved boolean DEFAULT true,
  flagged_reason text,
  moderated_at timestamp with time zone,
  moderated_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  published_at timestamp with time zone DEFAULT now(),
  CONSTRAINT posts_pkey PRIMARY KEY (id),
  CONSTRAINT posts_author_id_fkey FOREIGN KEY (author_id) REFERENCES public.users(id),
  CONSTRAINT posts_moderated_by_fkey FOREIGN KEY (moderated_by) REFERENCES public.users(id)
);
CREATE TABLE public.project_user_assignments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  project_id uuid NOT NULL,
  milestone_id uuid,
  ngo_user_id integer NOT NULL,
  assigned_by integer,
  assignment_role text NOT NULL DEFAULT 'field_staff'::text,
  assignment_status text NOT NULL DEFAULT 'active'::text,
  assigned_at timestamp with time zone DEFAULT now(),
  removed_at timestamp with time zone,
  notes text,
  CONSTRAINT project_user_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT project_user_assignments_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.csr_projects(id),
  CONSTRAINT project_user_assignments_milestone_id_fkey FOREIGN KEY (milestone_id) REFERENCES public.csr_project_milestones(id),
  CONSTRAINT project_user_assignments_ngo_user_id_fkey FOREIGN KEY (ngo_user_id) REFERENCES public.users(id),
  CONSTRAINT project_user_assignments_assigned_by_fkey FOREIGN KEY (assigned_by) REFERENCES public.users(id)
);
CREATE TABLE public.service_clients (
  id integer NOT NULL DEFAULT nextval('service_clients_id_seq'::regclass),
  service_offer_id integer NOT NULL,
  client_id integer NOT NULL,
  message text,
  proposed_amount numeric,
  proposed_start_date date,
  status character varying DEFAULT 'pending'::character varying,
  applied_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  expected_beneficiaries integer,
  project_timeline text,
  start_date date,
  end_date date,
  CONSTRAINT service_clients_pkey PRIMARY KEY (id),
  CONSTRAINT service_clients_service_offer_id_fkey FOREIGN KEY (service_offer_id) REFERENCES public.service_offers(id),
  CONSTRAINT service_clients_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.users(id)
);
CREATE TABLE public.service_offers (
  id integer NOT NULL DEFAULT nextval('service_offers_id_seq'::regclass),
  ngo_id integer NOT NULL,
  title character varying NOT NULL,
  description text NOT NULL,
  category character varying NOT NULL,
  skills_offered jsonb,
  pricing_info jsonb,
  availability jsonb,
  location character varying,
  status character varying DEFAULT 'active'::character varying,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  admin_status USER-DEFINED DEFAULT 'pending'::admin_status_enum,
  admin_reviewed_at timestamp with time zone,
  admin_reviewed_by integer,
  admin_comments text,
  submitted_for_review_at timestamp with time zone DEFAULT now(),
  CONSTRAINT service_offers_pkey PRIMARY KEY (id),
  CONSTRAINT fk_service_offers_admin_reviewed_by FOREIGN KEY (admin_reviewed_by) REFERENCES public.users(id),
  CONSTRAINT service_offers_ngo_id_fkey FOREIGN KEY (ngo_id) REFERENCES public.users(id)
);
CREATE TABLE public.service_requests (
  id integer NOT NULL DEFAULT nextval('service_requests_id_seq'::regclass),
  ngo_id integer NOT NULL,
  title character varying NOT NULL,
  description text NOT NULL,
  category character varying NOT NULL,
  tags jsonb,
  urgency_level USER-DEFINED DEFAULT 'medium'::urgency_level_enum,
  volunteers_needed integer DEFAULT 1,
  priority USER-DEFINED DEFAULT 'medium'::priority_enum,
  deadline date,
  location character varying,
  requirements jsonb,
  image_url character varying,
  status USER-DEFINED DEFAULT 'active'::service_status_enum,
  volunteer_limit integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  timeline character varying,
  contact_info text,
  CONSTRAINT service_requests_pkey PRIMARY KEY (id),
  CONSTRAINT service_requests_ngo_id_fkey FOREIGN KEY (ngo_id) REFERENCES public.users(id)
);
CREATE TABLE public.service_volunteers (
  id integer NOT NULL DEFAULT nextval('service_volunteers_id_seq'::regclass),
  service_request_id integer NOT NULL,
  volunteer_id integer NOT NULL,
  application_message text,
  status character varying DEFAULT 'pending'::character varying,
  applied_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  volunteer_hours numeric,
  team_size integer DEFAULT 1,
  team_members text,
  impact_statement text,
  estimated_impact_value numeric,
  CONSTRAINT service_volunteers_pkey PRIMARY KEY (id),
  CONSTRAINT service_volunteers_service_request_id_fkey FOREIGN KEY (service_request_id) REFERENCES public.service_requests(id),
  CONSTRAINT service_volunteers_volunteer_id_fkey FOREIGN KEY (volunteer_id) REFERENCES public.users(id)
);
CREATE TABLE public.trending_topics (
  id bigint NOT NULL DEFAULT nextval('trending_topics_id_seq'::regclass),
  topic character varying NOT NULL UNIQUE,
  category character varying,
  mention_count integer DEFAULT 1,
  unique_users_count integer DEFAULT 1,
  trend_score numeric DEFAULT 0.0,
  last_mentioned_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT trending_topics_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_addresses (
  id bigint NOT NULL DEFAULT nextval('user_addresses_id_seq'::regclass),
  user_id bigint NOT NULL,
  name character varying NOT NULL,
  address_line_1 text NOT NULL,
  address_line_2 text,
  city character varying NOT NULL,
  state character varying NOT NULL,
  pincode character varying NOT NULL,
  country character varying DEFAULT 'India'::character varying,
  phone character varying NOT NULL,
  address_type character varying DEFAULT 'home'::character varying CHECK (address_type::text = ANY (ARRAY['home'::character varying, 'work'::character varying, 'other'::character varying]::text[])),
  is_default boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_addresses_pkey PRIMARY KEY (id),
  CONSTRAINT user_addresses_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_connections (
  id bigint NOT NULL DEFAULT nextval('user_connections_id_seq'::regclass),
  follower_id integer NOT NULL,
  following_id integer NOT NULL,
  connection_type character varying DEFAULT 'follow'::character varying,
  status character varying DEFAULT 'active'::character varying,
  relationship_note text,
  interaction_level integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_connections_pkey PRIMARY KEY (id),
  CONSTRAINT user_connections_follower_id_fkey FOREIGN KEY (follower_id) REFERENCES public.users(id),
  CONSTRAINT user_connections_following_id_fkey FOREIGN KEY (following_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_notifications (
  id bigint NOT NULL DEFAULT nextval('user_notifications_id_seq'::regclass),
  user_id integer NOT NULL,
  type character varying NOT NULL,
  title character varying NOT NULL,
  message text,
  related_user_id integer,
  related_post_id bigint,
  related_comment_id bigint,
  is_read boolean DEFAULT false,
  is_seen boolean DEFAULT false,
  action_url character varying,
  created_at timestamp with time zone DEFAULT now(),
  read_at timestamp with time zone,
  seen_at timestamp with time zone,
  CONSTRAINT user_notifications_pkey PRIMARY KEY (id),
  CONSTRAINT user_notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT user_notifications_related_user_id_fkey FOREIGN KEY (related_user_id) REFERENCES public.users(id),
  CONSTRAINT user_notifications_related_post_id_fkey FOREIGN KEY (related_post_id) REFERENCES public.posts(id),
  CONSTRAINT user_notifications_related_comment_id_fkey FOREIGN KEY (related_comment_id) REFERENCES public.post_comments(id)
);
CREATE TABLE public.users (
  id integer NOT NULL DEFAULT nextval('users_id_seq'::regclass),
  email character varying NOT NULL UNIQUE,
  password character varying NOT NULL,
  user_type USER-DEFINED NOT NULL,
  name character varying NOT NULL,
  phone character varying,
  location character varying,
  verified boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  email_verified boolean DEFAULT false,
  phone_verified boolean DEFAULT false,
  two_factor_enabled boolean DEFAULT false,
  two_factor_secret character varying,
  account_status USER-DEFINED DEFAULT 'pending_verification'::account_status_enum,
  last_login timestamp with time zone,
  login_attempts integer DEFAULT 0,
  locked_until timestamp with time zone,
  timezone character varying DEFAULT 'UTC'::character varying,
  preferences jsonb,
  privacy_settings jsonb,
  verification_status USER-DEFINED DEFAULT 'unverified'::verification_status_enum,
  verification_level USER-DEFINED DEFAULT 'basic'::verification_level_enum,
  verified_at timestamp with time zone,
  identity_verified boolean DEFAULT false,
  city character varying,
  state_province character varying,
  pincode character varying,
  country character varying DEFAULT 'India'::character varying,
  email_verified_at timestamp without time zone,
  phone_verified_at timestamp without time zone,
  age integer,
  work_experience text,
  industry character varying,
  website character varying,
  company_size character varying,
  ngo_size character varying,
  profile_image text,
  profile_data jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);